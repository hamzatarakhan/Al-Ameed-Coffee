// Default (Android/web): no custom background — tabBarStyle's solid
// backgroundColor shows through. iOS gets the translucent blur variant
// from tab-bar-background.ios.tsx instead (native tab bar convention).
export default function TabBarBackground() {
  return null;
}
